
#include "lib.h"

float numeromax ( float numeri[], int dim) {
    numeri [dim];
    float massimo= numeri[0];
    for (int i = 1; i < dim; i++) {
        if (massimo < numeri[i]) {
            massimo = numeri[dim];
        }
    }

}
